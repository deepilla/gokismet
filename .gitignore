# Config file for testing. Contains a (private) Akismet API key
testconfig.json
# Binary created by "go test -c"
gokismet.test
# Test coverage files
# (assuming standard naming convention from http://blog.golang.org/cover)
*.out
